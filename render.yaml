services:
  - type: web
    name: jurisgen-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: JUREMA_PROVIDER
        value: hf
      - key: HF_API_KEY
        sync: false
      - key: CLAUDE_CREDENTIALS_B64
        sync: false
