name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install frontend dependencies
        run: cd frontend && npm install --include=dev

      - name: Build Frontend
        working-directory: frontend
        run: npm run build
        env:
          VITE_API_BASE: ${{ secrets.VITE_API_BASE }}

      - name: Verify build output
        run: ls -la frontend/dist/

  deploy-backend:
    needs: build-frontend
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Trigger Render Deploy
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK_URL" ]; then
            echo "RENDER_DEPLOY_HOOK_URL not configured — skipping (Render autoDeploy handles it)"
            exit 0
          fi
          curl -s -X POST "$RENDER_DEPLOY_HOOK_URL"
        env:
          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
          CURL_CA_BUNDLE: /etc/ssl/certs/ca-certificates.crt

# NOTE: Para ativar este passo:
# 1. Abra https://dashboard.render.com → jurisgen-backend → Settings → Deploy Hooks
# 2. Copie a URL do "Deploy Hook" (formato: https://api.render.com/v1/hooks/standard/xxx)
# 3. No GitHub: Settings → Secrets and variables → Actions → New repository secret
# 4. Nome: RENDER_DEPLOY_HOOK_URL, Valor: cole a URL do passo 2
