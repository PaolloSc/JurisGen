# Backend API URL
# Deixe VAZIO quando nginx estiver proxiando /api/ (Docker, VPS, Cloudflare Tunnel via nginx)
# Preencha APENAS se o backend estiver em domínio separado do frontend
# Exemplo: VITE_API_BASE=https://api.jurisgen.com
VITE_API_BASE=
